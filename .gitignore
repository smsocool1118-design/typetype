node_modules/
dist-electron/
release/
release-dictionary/
.omx/
.DS_Store
.claude/
typetype.log

# Models (too large for GitHub)
resources/models/
resources/translation-models/

# Official ARM64 Electron runtime — downloadable, and electron.exe (168MB) exceeds
# GitHub's 100MB limit. Obtain per docs/arm64-build.md. (vendor/sherpa-onnx-win-arm64/
# IS tracked — it's custom-compiled and has no official ARM64 npm package.)
vendor/electron-arm64/
